using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI; //Libreria
/*
public class BarraVida : MonoBehaviour
{
    //Este script es para modifical el valor de la barra

    private Image _barra; //Declarar componentes propio con el _

    [SerializeField]
    private Vida vida; //Aceder a la clase vida para obtener el valor para la barra

    // Start is called before the first frame update
    void Start()
    {
        _barra = GetComponent<Image>(); //Obtener el objeto barra
    }


    // Update is called once per frame
    void Update()
    {
        _barra.fillAmount = vida.value/100; //Asignar el valor de la barra
    }
}
*/